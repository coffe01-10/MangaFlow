**已修复（round 3 集成批次）**：删除 `project-workspace.tsx` 深链判定中的 `pages.data.length > 0` 前置；零页章节时横幅同样出现，且文案按场景区分（零页章节不再谎称"已显示本章第 1 页"，改提示本章还没有任何页面）。

- 提交：0ab3d86。
- 验证：vitest 新增两条——有页章节缺目标页→横幅出现；零页章节+深链→横幅出现且文案为"本章还没有任何页面"、不再出现"已显示本章第 1 页"。apps/web 全量 484 项过（代理）。
- NOT RUN：Playwright e2e 深链实机路径。
