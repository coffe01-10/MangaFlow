# Desktop 流程编排布局验证

日期：2026-09-21。截图使用离屏 WPF 与测试数据。

## 已完成

- F11 全屏隐藏窗口边框、导航、顶栏及任务快捷栏；Esc 恢复原窗口状态。
- 专注模式可与全屏组合，恢复此前面板状态和属性编辑控件。
- 视图菜单统一切换节点库、属性、版本状态、运行面板及小地图。
- Shift+滚轮横向滚动，Ctrl+滚轮缩放，滚动范围随缩放和节点位置更新。
- 采用工作台共享纸色、墨色、朱红主题；默认 100% 缩放，增大节点标题及端口文字。
- 统一「流程编排」入口名称，当前页面隐藏重复导航，保留单一的运行工作流入口。

## 验证结果

- Release 原生项目及测试项目编译通过，0 错误；现有编译警告保留，见 build.log。
- 原生工作流回归通过：650/940/1100/1440 布局、窗口全屏恢复、专注模式、提示可见性、面板恢复、滚轮、缩放范围、草稿保存、撤销重做、连线、运行配置、版本恢复与切换竞态。
- 原生侧栏回归通过：1320/960 宽度、860/640/600 高度、导航、长项目名、折叠状态恢复。
- npm run check 通过：ESLint、Ruff、脚本类型检查；Python 1664 passed / 47 skipped；Vitest 56 文件、615 passed；Next.js 生产构建通过。详见 check.log。
- git diff --check 通过。

## 验证边界

原生界面通过离屏渲染、WPF 控件事件和 HTTP fixture 验证。未进行实机鼠标拖拽、多显示器/高 DPI、帧时间或真实 Provider/Worker 验收。没有打包或替换已安装客户端。

正式本地构建：apps/desktop/native/bin/Release/net10.0-windows/MangaFlow.Native.exe。
