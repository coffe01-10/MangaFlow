# WPF 迁移统一待办与修复交接

核对日期：2026-09-14。源码基线：d11d55b143c87c7dc0263b9b0000fb70c88c63ef，包含当前系统设置、用量页面未提交修改。

范围为本任务网页到 WPF 迁移的已知问题与验收缺口，不是全仓所有潜在缺陷的穷尽审计。本轮只读核对并整理记录，没有修复业务代码、执行真实后端复现或重新构建。源码风险、交互差异、未验收分别列出，不能互相替代。

## A. 仍需修复的流程与数据问题

| 编号 | 页面 | 当前问题与影响 | 验收要求 |
| --- | --- | --- | --- |
| A01 | 流程编排 | SaveNowCoreAsync 捕获失败不向调用方传递失败；校验、发布和切换仍可能继续，ConfirmLeaveAsync 最终返回 true。存在未保存草稿丢失或发布旧草稿的风险。 | 延迟/409/断网保存失败后，禁止继续发布和离开；保留原工作流与草稿；维持保存串行及 A→B→C 归属保护。 |
| A02 | 项目设置 | 保存期间可继续编辑，响应后无条件 dirty=false，新增编辑未保存却不再提示。 | 提交 2 后改 8，响应回来仍保留 8 且标脏；第二次保存携带新版本。 |
| A03 | 项目设置 | 409 后版本未更新，提示刷新，但 RefreshAsync 又被 dirty 守卫阻止。 | 保留草稿，提供可执行的冲突恢复；不盲目覆盖远端、不用取消草稿守卫应付。 |
| A04 | 项目设置 | ModelOption 丢失 catalog_id，新目录模型可能被写到 text_model_alias，而不是 default_text_model_id。 | 覆盖目录模型、旧 alias、已隐藏模型、auto；正确写入并验证模型字段。不能据此认定所有模型调用均失败。 |
| A05 | 系统设置 | SaveRuntime 成功后 RenderRuntime 重建输入，请求期间的新编辑被旧响应覆盖。 | 提交期间的新编辑保留、继续标脏，后续保存使用新版本。 |
| A06 | 系统设置 | 409 只显示文字错误；有草稿时刷新跳过，不能安全恢复版本。 | 冲突后保留编辑且有有效恢复和重试入口。 |
| A07 | 系统设置 | 没有覆盖 ConfirmLeaveAsync；基类直接允许离开，主窗口导航和关闭依赖该方法。 | 返回项目、进入用量页、其他导航和关窗均保护运行设置与连接草稿；取消离开无数据丢失。 |
| A08 | 系统设置 | 搜索、筛选、排序等 RenderProviders 清空并重建连接表单，可能丢失正在输入的密钥或手工模型。 | 保留草稿或切换前确认；取消不丢稿，不能输出真实密钥。 |
| A09 | 系统设置 | 状态条依赖并行请求完成顺序；diagnostics 先完成时可能停在“读取中”，失败可能留旧状态。 | 分别延迟 providers/runtime/diagnostics，覆盖空列表、失败和重试；每项状态准确。 |
| A10 | 用量看板 | 汇总请求不传 channel，明细传 channel；同一筛选下统计卡、趋势与明细可能口径不同。后端当前已支持该参数。 | 汇总和明细都按所选通道过滤；更新“summary 不含 channel”的过时测试。 |
| A11 | 用量看板 | CSV 仅导出 groups，不含 billed；金额使用当前文化 ToString，逗号小数地区可能破坏列。 | 补账单区块，固定数值格式；覆盖只有账单、多币种、未知金额、中文、换行、引号和公式前缀。 |
| A12 | 用量看板 | 供应商/模型选项从当前汇总增量提取、只增不清，缺独立维度来源与供应商联动。 | 时间/项目/供应商切换后选项完整且无残留，清除不兼容模型，维度读取失败可见可重试。 |
| A13 | 用量看板 | 项目列表初次读取失败中止加载，页面数据刷新不重新获取项目列表。 | 项目列表有有效重试，不能把读取失败当作“没有项目”。 |
| A14 | 用量看板 | summary/attempts 的 Task.WhenAll 任一失败阻止另一块渲染；加载更多的错误只显示在页面顶部。 | 成功分区保留，失败分区就地提示和重试；保持分页去重、筛选快照及旧响应隔离。 |
| A15 | 公共 API 客户端 | HTTP 错误最终转成 InvalidOperationException 文本，未保留结构化状态。场景引用检查等无法可靠区分资源不存在和读取失败。 | 保留状态码和结构化详情，针对调用点区分 404/409/网络错误；保留已有错误提示、超时和取消语义。 |

定位文件：apps/desktop/native/Views/WorkflowView.cs、ProjectSettingsView.cs、SettingsView.cs、UsageView.cs；apps/desktop/native/Models.cs；apps/desktop/native/Services/UsageFilter.cs、ApiClient.cs。A 项为当前源码核查，不冒充真实窗口复现。

## B. 已记录但仍未与网页对齐的功能和交互

| 编号 | 页面 | 待补齐内容 |
| --- | --- | --- |
| B01 | 流程编排 | 发布版本列表与历史版本恢复，包括覆盖草稿确认、版本冲突及切换后的旧响应隔离。 |
| B02 | 流程编排 | 页面范围的所属章节选择；当前固定取第一章，不能正常选择后续章节页面。 |
| B03 | 流程编排 | 第二个默认模板 chapter_export；当前空列表只创建 manga_default。 |
| B04 | 流程编排 | 失败运行重试入口、请求防重和重试后状态更新；不可自动触发付费重试。 |
| B05 | 流程编排 | 画布小地图。 |
| B06 | 流程编排 | 框选与多节点操作。 |
| B07 | 流程编排 | 完整画布手势及全屏工作区外壳的网页对照还原。 |
| B08 | 系统设置 | 供应商分组折叠；当前分组是静态标题。 |
| B09 | 系统设置 | 搜索覆盖模型目录；当前只查供应商名称、预设键、连接协议。 |
| B10 | 系统设置 | 新增供应商对齐网页页内表单；当前为对话框，迁移时保护草稿生命周期。 |
| B11 | 漫画剧本 | 场景绑定和服装修改仍需显式保存，网页下拉变更即保存；需保留完整服装映射及失败恢复。 |
| B12 | 分页与分镜 | 本格完整属性仍用原生编辑对话框，网页为内联编辑。 |
| B13 | 单页生成 | 导演子页继续逐项还原，包括提议、澄清、命令差异、确认/拒绝、撤销/重做。已有实现不能被误写成完全不存在。 |
| B14 | 单页生成 | 场景继承专用卡继续对照还原。 |
| B15 | 用量看板 | 调用详情当前为 Window/模态交互，尚未对齐网页侧边抽屉；字段逐项完整性还需对照验收。 |
| B16 | 全局任务底栏 | 项目并发上限尚未展示，展开内容与网页仍有差异。迁移记录明确该字段尚未接入。 |

B05–B07、B13–B14 依据最新逐页记录，不表示其所有子能力均无代码；后续应按网页组件拆解验收。浏览器 URL 重定向与原生深链接的兼容规则另需明确设计和验收，不能因为普通页面导航可用就认定深链接已迁移。

## C. 必须补齐的验收（未验收不等于已确认故障）

1. **全页视觉对照**：同一项目、同一数据和视口比较完整 WPF 窗口与网页，覆盖标题栏、侧栏、底栏、正文、弹层、空/错/加载/成功状态；核对所有矩形按钮、字体、间距与图标。现有多数截图仅为离屏页内容。
2. **字体清晰度**：100/125/150/200% 缩放、跨显示器移动、最大化/还原、滚动后静止状态。现有 Display/ClearType/实色背景/PerMonitorV2 修改不能证明用户所见发糊已完全消除。
3. **动画与性能**：真实首帧、切页响应、滚动、分镜和工作流拖拽/缩放、长列表内存、长时间运行。早期仅三个句柄/输入空闲启动样本，不是首帧或帧率验收。
4. **17 个页面真实 WPF 操作**：逐项点击并核对 API 持久化，包含文件选择、真实拖放、图片预览、确认框、Enter/Esc、焦点恢复；mock 和直接 HTTP 检查不等于这项通过。
5. **生成闭环**：原文解析→剧本→分页→角色/服装/风格/页面生成→任务完成刷新→候选采纳→质检/修复/升清→导出；实际供应商权限与产物未全链路验收。
6. **队列与异常**：实际采用的 LOCAL/AUTO/RQ 环境中取消、重试、断网、超时、客户端重启恢复、版本冲突及晚到响应。未启用 RQ 本身不证明 LOCAL 生成失效。
7. **供应商配置与费用**：真实连接验证、模型发现/启停/价格、凭据保存、预算、用量统计与调用明细一致性；不擅自调用付费服务。
8. **真实文件输出**：CSV 保存及内容、整章导出下载、取消/写入失败/中断清理；现有部分本地文件测试不代表运行中后端全部通过。
9. **WPF 发布交付**：脱离开发环境的依赖打包、安装/升级/卸载、签名、实际加载宿主路径与哈希、异常退出与子进程清理。旧壳交付证据不能直接替代 WPF 发布验收。
10. **当前版本统一回归**：定向回归、全仓检查和现有构建警告梳理。旧报告中的 WinError 32、旧分支测试失败需当前 SHA 复查，不能直接认定仍失败。较晚报告已覆盖部分早期矩阵缺项，应按案例去重登记。

## D. 各页面的剩余责任

| 页面 | 仍需处理 |
| --- | --- |
| 项目首页 | C 类实际创建/导航/封面、完整窗口视觉及性能验收；本轮无新增确定缺陷。 |
| 帮助 | C 类锚点/返回/键盘焦点和实机排版验收；本轮无新增确定缺陷。 |
| 原作与修订 | 实际控件导入/修订/删除和解析分页闭环；有早期真实 HTTP 证据，但不等于当前全部控件验收。 |
| 人物设定与角色包 | 已修问题回归、真实概念生成/采纳、历史规格和版本交互全链路验收。 |
| 服装档案 | 草稿保护回归、真实穿着图生成/采纳、文件选择与拖放验收。 |
| 场景资产 | A15；真实窗口归档引用、变体、图片操作与恢复验收。 |
| 漫画风格 | 真实生成结束→人工通过→正式激活成功闭环。 |
| 原始参考素材 | 真实拖放/选择/预览/改名/分类/绑定等鼠标操作。 |
| 漫画剧本 | B11，以及真实解析结果到编辑/持久化验收。 |
| 分页与分镜 | B12，以及真实手势、编辑/保存冲突和性能验收。 |
| 单页生成（含导演/局部编辑） | B13–B14；实际生成、局部选区候选/血缘、采纳/撤回、质检/升清及导出闭环。 |
| 生成素材库 | 实际图片网络加载、收藏/撤回/删除、筛选/分页及整章导出。 |
| 任务中心 | 真实执行、取消/重试、历史/归档、结果预览和调用账本。 |
| 流程编排 | A01、B01–B07。 |
| 项目设置 | A02–A04，以及真实保存/删除和离开保护。 |
| 系统设置 | A05–A09、B08–B10。 |
| 用量与成本 | A10–A14、B15，以及真实统计和文件导出验收。 |

## E. 不再重复列为“未修”的旧问题

2026-09-09 后端连通性报告中的以下问题，当前代码已有对应修复实现；本轮未重复运行它们的实机验收：

- 人物参考图/概念图预览：调用 ShowImage 已传相对路径，原双重 OriginUrl 转换已改变。
- 概念图刷新：AssetsView.PollTick 已转发 CharactersPane → ConceptPanel。
- 角色包冻结规格：CharacterPackagePane 已有 RenderFrozenSpec，并读取 spec_snapshot。
- 服装离开保护：AssetsView 已接 GuardOutfitDraftAsync；不因此推断其他所有资产草稿都有同等保护。
- 宿主构建部署：start-native.ps1 已检查构建退出码、复制哈希；NativeBackend 记录实际路径和 SHA256。不要重复提交同类修复。

早期 n1/n2 核心、宿主和 sidecar 审计含大量后续已修条目；不能摘取其中“遗留”字样就作为当前待修。此清单不对其全仓历史条目重新作缺陷定级。

## F. 交给其他 AI 的执行提示词

请在 D:\自媒体\漫画工作流 处理 WPF 网页迁移遗留问题。先读 AGENTS.md、git status/diff 和 output/native-repair-backlog-2026-09-14.md。工作区存在系统设置、用量等未提交改动，禁止覆盖或回滚。

先逐项确认 A 类，优先 A01–A08 的保存和草稿安全，再处理统计、错误恢复及 B 类迁移。每个修复写明编号、复现、接口契约、行为变更和验收证据；按独立责任拆分，不一口气重构全部页面。保留原生 WPF、网页布局、矩形按钮及文字清晰度措施，不用 WebView 替代。

详细验收参考 output/workflow-page-review/handoff-prompt.md、output/project-settings-page-review/handoff-prompt.md、output/system-settings-page-review/handoff-prompt.md、output/usage-page-review/handoff-prompt.md。这些文件基线可能早于当前提交，以当前代码复核为准。

本任务优先修客户端；发现必须改后端时先提交具体证据与独立方案，不擅自修改事务、数据库迁移、队列、Worker 生命周期或供应商执行。不操作用户已有业务数据，不读取/输出凭据，不自动进行付费生成。临时文件及时清理。

每个行为变更补失败回归，运行相关原生专项及既有保护测试，交付前运行与风险匹配的统一检查。C 类分别记录真实环境和 NOT RUN，禁止把 mock、离屏截图、编译成功或直接 HTTP 调用冒充所有按钮全链路验收。D/E 用于防遗漏与避免重复修复；不得把“未验收”直接写成“功能坏了”。
