[pytest]
testpaths = tests
; Desktop sidecar e2e lives under apps/desktop and is opt-in via
; apps/desktop/scripts/run-sidecar-e2e.sh. Default npm run test / check
; must not collect it (POSIX killpg, live sidecar).
norecursedirs = .git .venv node_modules apps/desktop
