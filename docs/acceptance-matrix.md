# MangaFlow AI MVP 验收矩阵

更新时间：2026-07-15

本矩阵把产品目标映射到当前可重复验证的证据。完整自动化入口为 `npm run check`，真实 Vertex 调用不属于默认测试，避免意外费用。

| 产品要求 | 当前证据 | 结论 |
| --- | --- | --- |
| 粘贴、TXT、Markdown 导入完整小说原文 | `test_lossless_import_and_dynamic_pagination`、`test_txt_and_markdown_source_upload` | 通过 |
| AI 自动生成完整剧本并补足可视化细节 | `test_story_parse_worker_writes_complete_traceable_script`、`test_1500_to_3000_character_full_manga_acceptance` | 通过 |
| 原文、Scene、Beat、对白、页面可追溯且不得漏段 | 统一闭环覆盖率为 100%；缺失剧本或来源时接口返回 409 | 通过 |
| 内容密度决定页数，不把长章节压成固定页数 | 动态分页测试验证长文本产生更多页，单页不超过 180 字、8 个气泡 | 通过 |
| 每页 3–7 格、右到左日式分格 | 分页测试及持久化分镜测试检查格数、阅读方向、格位和阅读顺序 | 通过 |
| Nano Banana 2 与 Nano Banana Pro 平级可选 | API 中每个候选显式记录模型；浏览器显示两个同级按钮 | 通过 |
| 可选择一次生成数量并受 1–8 路并发限制 | UI 候选数量选择、本地 Worker 8 任务隔离与项目并发峰值测试 | 通过 |
| 主要姓名、绰号、人物参考图绑定 | 角色冲突/绑定测试及统一闭环；剧本说话人规范化为主要姓名 | 通过 |
| 自动补全角色正面、侧面、背面、表情资料 | 统一闭环实际执行四个假模型资产任务并得到四个 READY 候选 | 通过 |
| 服装参考生成角色穿着图，场景服装可指定 | 统一闭环生成服装候选、为所有场景指定服装并进入页面提示 | 通过 |
| 漫画参考图自动总结并模仿画风 | 统一闭环执行多模态风格分析、激活风格并生成风格测试图 | 通过 |
| 自动、半自动、导演模式可选 | 项目模型、提示编译器和浏览器工作模式选择 | 通过 |
| 角色、服装、画风一致性锁 | 角色固定/禁止改变项、服装与风格锁定项进入提示；锁冲突测试阻止修复 | 通过 |
| 单页抽卡、跨模型比较、收藏多个、采用一个、进入下一页 | 候选生命周期测试及统一闭环逐页采用 | 通过 |
| 按章节、页面、批次管理素材并支持筛选和软删除 | 素材库接口/UI及角色、类型、模型、清晰度、日期、收藏筛选测试 | 通过 |
| 文字、说话人、角色、服装、道具和连续性检查 | 统一闭环为首个候选生成六类检查结果 | 通过 |
| 文字区域→气泡→单格→整页递增修复，最多三次 | 修复升级测试与锁规则测试 | 通过 |
| 单页 PNG、整章 PNG/PDF、项目 JSON 与素材清单 | 统一闭环下载并解析 ZIP、PDF、JSON，JSON 含 `asset_manifest` | 通过 |
| 专用 Web UI | Next.js 生产构建通过；Codex 内置浏览器验证七步工作流且无浏览器错误 | 通过 |
| 服务端 Vertex 凭据与真实可用性 | 密钥被 Git 忽略；低 token Gemini 文本和一张 Nano Banana 2 1K 图片烟雾测试成功 | 通过 |

## 运行边界

- 统一闭环使用确定性假模型，覆盖全部业务状态与文件产物，不产生 Vertex 费用。
- 真实 Vertex 仅执行计划允许的低 token 文本与单张 1K 图片烟雾测试；没有自动生成整章真实付费图片。
- 复杂图层、多用户协作、移动端和无人值守整章生成不属于 MVP。
