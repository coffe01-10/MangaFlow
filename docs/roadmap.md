# MangaFlow 主分支后续工作清单

更新时间：2026-08-27
最近全量验收基线：`master` / `cb324e3`（2026-08-27；相对 PR #5 合并提交 `d5d32ec` 仅增加任务文档），`check:full` 已通过。
本轮深审基线：`master` / `f085327`（相对上述验收基线仅改文档）。新增 6 项 P1 修复与 1 项 P2 改进，全部待完成；P0-1 的可靠性闭环重新打开，不能用历史门禁通过替代新增竞态验收。

## 当前判断

MangaFlow 已经具备私有单用户漫画生产工作台的主要功能面：原作导入、结构化剧本、动态分页与分镜、人物/服装/风格资产、单页生成、人工采用、视觉检查、修复、多供应商模型平台、DAG 工作流和多种导出均有代码与自动测试。

当前不应再按“补齐 MVP 页面”推进，而应进入可靠性收口。页面生产门禁、旧候选复检、Worker 租约、启动迁移校验、活动任务轮询和浏览器基线修复均已合并，正常流程的离线功能与浏览器门禁已独立通过。但本轮深审确认取消后调用、状态覆盖、调度中断、草稿丢失和质检误放行等实质问题，持续生产验收仍有阻塞。优先完成 P1-7～P1-12，再处理 P2-8 并发发布、其余序号竞态与独立性能门禁。真实供应商闭环仍需用户另行授权，不把离线通过等同于全部生产验收完成。

最新验收证据与边界：

- 2026-08-27 在合并后主分支 `cb324e3` 独立执行 `npm run check:full`，退出码 0：165 个 Python 测试、17 个 Vitest 测试、Ruff、ESLint、TypeScript 与 Next.js 生产构建全部通过。
- Playwright/Axe E2E 4/4 独立通过（23.3 秒）：统一导航、项目深链/后退/刷新/工作流入口、5 条路由首屏请求预算、7 条路由 Axe serious/critical 检查均通过。2026-08-26 的 2/4 为历史失败基线。
- 全新隔离 SQLite 从空库迁移到 `20260801_16 (head)`；7 项迁移测试、假模型完整漫画流程与默认 DAG 至输出测试均通过。验收结束外键检查为零，浏览器测试库无生成任务、调用记录或供应商密钥；临时数据库、素材、占位凭据与测试结果已清理，测试服务已退出。
- 环境前提：首轮使用不存在的凭据文件时有 10 项假模型相关测试因供应商停用失败（155 项通过）；改用不含有效凭据的临时空 JSON 满足“文件存在”条件后全绿，未修改业务代码或测试断言。该环境依赖记入 P2-2；15 条 Python 弃用警告不影响通过。
- 未执行真实供应商或 Vertex 图片调用，避免产生费用；真实凭据、代理、当前模型可用性和付费响应仍需独立验收。
- PR #2 的生产门禁、服装档案布局和旧候选复检继续保留；原先缺失的 Worker 租约、崩溃恢复和迁移校验已由 `d965007`～`390ebdc` 系列修复进入主分支。

## 2026-08-27 深审新增 TodoList

以下 7 项均为待修复事项，不代表已实现；编号与 `development-progress.md` 保持一致。这里的 P1 阻塞指持续生产验收，不是项目无法启动。

- [ ] **P1-7：取消与租约失效后禁止继续调用供应商或覆盖任务状态**（审查第 1 项）。
- [ ] **P1-8：工作流自动保存不丢修改，发布必须等待最新草稿保存成功**（审查第 2 项）。
- [ ] **P1-9：Redis 入队状态回写不得覆盖 Worker 已推进的状态**（审查第 3 项）。
- [ ] **P1-10：LOCAL/AUTO 回退路径真正执行可重试失败的后续调度**（审查第 4 项）。
- [ ] **P1-11：REDIS 多 Worker 等待并发名额时保留后续调度机会**（审查第 5 项）。
- [ ] **P1-12：空质检、缺项质检和失败记录不得被生产门禁放行**（审查第 6 项）。
- [ ] **P2-8：同一工作流并发发布的 revision 冲突受控处理**（审查第 7 项；关联 P1-5）。

复现证据与边界：深审完成 7 个后端隔离场景和 2 个前端保存时序场景；后端使用临时 SQLite、独立 Session 与假供应商/受控队列，前端执行从当前组件提取的实际回调，不是浏览器端到端复现。未运行真实 PostgreSQL、Redis 多进程或付费模型调用；未重跑全量门禁。已排除“达到最大次数仍能通过 API 手动重试”的假设，接口已有 409 拦截，不列为待办。复现脚本及临时数据库均已清理，后续修复需把场景固化为仓库回归测试。

## 2026-08-26 已合并修复

- 已建立统一的页面/章节 `production readiness`，并用于下一页、单页导出、整章导出和工作流输出。
- 生产通过的目标要求是候选仍被选中、素材存在、分镜版本已确认，并具备最新且通过的 `SPEAKER`、`CHARACTER`、`OUTFIT`、`PROP`、`CONTINUITY` 五类检查；本轮发现当前实现仅凭汇总状态放行，类别完整性尚未闭环，见 P1-12。
- 沿用旧候选会增加候选版本并强制产生新的检查幂等键，不能复用旧分镜的检查任务。
- 默认 DAG 以单页成品结束，整章导出使用独立流程并要求章节全部页面生产通过。

## 2026-08-27 完成项

- **P0-1 任务租约基础已合并，可靠性闭环重新打开**。提交 `d965007` 恢复遗留活动任务与过期租约并校验启动迁移版本；`2c17dc7` 修复 Worker token 与数据库并发竞态；`5427529` 为 PostgreSQL Worker 增加方言感知行锁；`33e4693` 在 claim 失败时执行锁内条件更新；`390ebdc` 为 RQ 区分可重试与终态失败。上述历史修复仍有效，但取消前调用检查、入队状态回写、LOCAL 重试和 RQ 槽位等待仍需 P1-7、P1-9～P1-11 收口。
- **P0-2 数据库启动以 Alembic 为准：已完成**。启动路径已移除 `create_all()`（仅留说明注释），启动时校验当前 revision 与仓库 head 一致。
- **P1-1 修复任务幂等插入竞态：已完成**。`worker_tasks.py` 以 `begin_nested()` savepoint 捕获 `IntegrityError` 后返回已存在任务，调用方事务不再被置为回滚。
- **P1-2 前端活动任务轮询：已完成（PR #4 已合并）**。新增共享模块 `apps/web/lib/task-status.ts` 统一活动/终态集合；工作台任务、候选、检查结果和资产生产面板的轮询全部接入，覆盖 `WAITING/QUEUED/PREPARING/UPLOADING_REFERENCES/GENERATING/OCR_CHECKING（历史兼容）/CONSISTENCY_CHECKING/REPAIRING/RUNNING`，检查与修复期间持续刷新、终态停止；新增单元测试与基于 TanStack Query 的轮询行为测试。
- **P1-3 浏览器验收基线、P1-4 完整检查入口：已完成（PR #5 已合并）**。首页摘要合入 Dashboard；对比度与可访问名称修复落地；新增 `check:full` 串联快速门禁与 Playwright/Axe；合并后主分支已独立复验通过。

## P0：基础修复已合并，P0-1 待补充闭环

### P0-1 任务租约、崩溃恢复与旧 Worker 防覆盖（重新打开）

当前状态：原子 claim、独立心跳、过期回收和终态条件更新已合并；本轮确认仍有中间状态覆盖及调度中断，补充修复与验收统一在 P1-7、P1-9～P1-11 跟踪，不重复创建实现任务。

历史问题：`GenerationJob` 已有 `lease_owner` / `lease_expires_at` 字段，但当时执行器没有原子 claim、租约心跳和过期回收。API 或 Worker 在付费调用期间退出后，活动任务可能永久卡住；同一任务被重复执行时，旧 Worker 仍可能覆盖新 Worker 的结果或终态。

工作：

- 以测试为依据，把 `699c190` 中的租约实现重新基于当前 `master` 整合，不直接覆盖已经合并的生产门禁、分镜焦点和生成素材导入改动。
- 原子 claim 任务并递增 `attempt_count`，确保并发 Worker 只有一个获得执行权。
- 长调用期间独立 heartbeat；只允许当前 `lease_owner` 刷新和落库。
- 启动时回收无租约的遗留活动任务和租约过期任务；达到 `max_attempts` 后统一收敛为失败。
- 取消、重试、失败和完成都清空租约；旧 Worker 丢失租约后不得写回候选、节点或工作流状态。
- 增加 `status + lease_expires_at` 索引和 Alembic 迁移。

验收：

- 覆盖双 Worker 竞争、心跳、API 重启、Worker 崩溃、过期回收、取消竞态、旧 Worker 晚返回、最大尝试次数耗尽和工作流同步失败。
- 本地执行器与 Redis/RQ 使用相同状态语义。
- 任何恢复测试不得触发第二次真实付费调用。

### P0-2 数据库启动必须以 Alembic 为准 ✅ 2026-08-27 完成

问题：API 启动仍调用 `Base.metadata.create_all()`。它会掩盖未初始化数据库，却不会把已有数据库可靠地升级到当前迁移头，容易让“服务能启动”与“数据库结构正确”产生分歧。

工作与验收：

- 应用启动时比较当前 Alembic revision 与仓库 head；不一致时失败并给出明确升级命令。
- 移除生产/开发启动路径中的 `create_all()`；测试夹具继续显式创建隔离 schema。
- 全新数据库、旧数据库逐步升级、降级后再升级和缺失 revision 都有自动测试。

## P1：稳定版本应完成

P1-1～P1-4 已完成，下列相应段落保留修复前问题与原验收标准；P1-5 的工作流发布冲突已复现并在 P2-8 跟踪，其他序号范围待确认；P1-6 离线功能门禁已有历史通过记录、独立性能门禁待完成；本轮新增 P1-7～P1-12 均未完成。

### P1-1 修复任务幂等插入竞态 ✅ 2026-08-27 完成

当前 `create_job` 采用“先查询、再插入唯一键”。两个并发请求可能都查不到记录，后到者在 `flush()` 处抛出 `IntegrityError`，并把调用方同一事务中的其他待提交数据一起置为回滚状态。

- 使用 nested transaction/savepoint 捕获唯一键竞争，失败后返回已存在任务。
- 回归测试同时验证幂等只生成一个任务，并且调用方在竞争前创建的数据不会被回滚。

### P1-2 修复前端活动任务轮询 ✅ 2026-08-27 完成

主工作台的任务列表已经认识 `UPLOADING_REFERENCES`、`CONSISTENCY_CHECKING`、`REPAIRING` 等状态，但查询轮询只覆盖 `WAITING`、`QUEUED`、`PREPARING`、`GENERATING`、`RUNNING`。任务进入检查或修复阶段后，页面可能停止刷新，直到用户手动刷新才显示结果。

- 提取前端共享的活动状态集合，工作台、候选、检查结果和资产生产面板共用。
- 至少覆盖 `WAITING`、`QUEUED`、`PREPARING`、`UPLOADING_REFERENCES`、`GENERATING`、`OCR_CHECKING`（历史兼容）、`CONSISTENCY_CHECKING`、`REPAIRING`、`RUNNING`。
- 增加组件测试，证明检查/修复期间继续轮询，进入终态后停止轮询。

### P1-3 恢复浏览器验收基线 ✅ 2026-08-27 完成

2026-08-26 的历史 Playwright 失败证据：

- 首页预算要求不超过 3 个 API 请求，实测 4 个：`/projects/dashboard`、`/settings/vertex/status`、`/models`、`/providers`。
- Axe 在首页发现 1 类严重问题、4 个节点：`.empty-project > small` 对比度 3.75:1；生产闭环第 6/7 步编号 2.32:1；`.honesty-note` 4.49:1，均低于普通文字 4.5:1 要求。

修复与结果：

- `/projects/dashboard` 新增 `ai_overview` 摘要（可用模型数、健康连接数、已配置连接数），首页删除独立的 `/models`、`/providers` 首屏请求，首屏降至 dashboard + vertex status 共 2 个请求。
- 对比度修复覆盖 Axe 实测暴露的全部页面：浅色系灰字统一替换为 `#66604f`（最差背景 5.03:1）；工作流深色面板灰绿统一为 `#a4ada7` / `#b8c0ba`（6.33:1+）；状态徽标琥珀/朱红改为 `#8f6117` / `#b23c25`（5.13:1+）；章节导航序号移除 `opacity:.6` 叠加。
- Workflow 工作台补充可访问名称：3 个 `<select>` 增加 aria-label（选择工作流、运行范围类型、运行目标），节点库/属性面板关闭按钮与校验清除按钮增加 aria-label，设置页手动模型类型下拉增加 aria-label。
- PR #5 作者重跑全部 4 项 Playwright E2E 通过；2026-08-27 合并后主分支 `cb324e3` 独立复验也为 4/4 通过。

工作与验收：

- 将首页所需的模型/供应商摘要并入 Dashboard，或明确重构请求，恢复不超过 3 个首屏 API 请求的既定预算。
- 调整对应文字颜色或字号/字重，确保 Axe 的 WCAG 2 AA serious/critical 结果为零。
- 重跑全部 4 项 Playwright E2E；随后再跑 Lighthouse 和 100 节点工作流 FPS 门禁。

### P1-4 把浏览器门禁纳入可见的完整检查 ✅ 2026-08-27 完成

`npm run check` 当前只包含 lint、Python 测试、Vitest 和生产构建，不包含 Playwright、Axe、Lighthouse 或工作流 FPS；文档中的“完整质量门禁”不能继续与 `npm run check` 混为一谈。

- 保留快速 `npm run check`，新增不会调用真实模型的 `npm run check:full` 或 CI 工作流。
- `check:full` 至少运行 Playwright/Axe；Lighthouse 与 FPS 可按稳定环境单独设门禁，避免机器波动导致假失败。
- CI 产物保留失败截图/trace，但本地临时数据库、浏览器结果和测试项目在运行后自动清理。

### P1-5 消除序号分配竞态

多处用 `MAX(ordinal) + 1` 分配章节、批次、候选或工作流 revision，同时数据库又有唯一约束。本轮已用两个独立 Session 复现同一工作流的发布 revision 冲突，按实际影响定为 P2-8；此处保留其他序号分配路径的范围核对，不把所有 `MAX + 1` 用法直接认定为已证实缺陷。

- [ ] 核对 `GenerationBatch(project_id, ordinal)`、`PageCandidate(batch_id, ordinal)`、`SourceRevision(chapter_id, revision)` 的真实并发入口与事务锁行为，以独立 Session 复现为准。
- [ ] 对确认存在竞态的路径采用数据库级分配、受控锁或唯一键冲突有限重试，并固化回归测试。
- `WorkflowVersion(workflow_id, revision)` 的已确认问题、修复与验收统一见 P2-8，避免重复计项。

### P1-6 主分支离线功能门禁已通过，独立性能门禁待完成

- [x] PR #2 及后续可靠性修复、PR #4 轮询修复、PR #5 浏览器基线修复已合并；本地主分支已同步至 `d5d32ec`。
- 保留主分支已经合并的 production readiness、旧候选复检和 storyboard focus 修复，不通过一次大范围文件覆盖来“解决冲突”。
- [x] 合并后主分支 `cb324e3` 已独立运行 `npm run check:full`：165 Python、17 Vitest、4 Playwright 全通过；包含迁移测试和假模型完整 DAG 至输出的证据。
- [ ] 在稳定环境补跑 Lighthouse 与 100 节点工作流 FPS 门禁。
- 删除或归档旧分支只能在主分支验证完成后进行。

### P1-7 取消与租约失效后禁止继续调用或覆盖状态（阻塞）

定位：`apps/api/app/worker_tasks.py:924`、`:940`（本轮源码行号基于 `f085327`，后续改动时以函数为准）。

已复现：参考图准备期间，另一个 Session 已提交取消；原 Worker 随后仍写入并提交 `GENERATING`，再调用一次假供应商，直到返回后才检查租约。最终任务带有取消时间和完成时间，却处于 `GENERATING` 且租约为空。不是已发请求无法撤回，而是取消完成后才发出请求。

- [ ] 所有相关中间状态更新校验当前租约所有权、有效期与取消状态；失去执行权的 Worker 不得覆盖任务或关联资源。
- [ ] 在供应商调用前检查执行权，取消或失租时终止后续派发；保留调用返回后的条件落库保护，并区分已经在途的请求。
- [ ] 回归测试在参考图准备与调用派发之间提交取消/租约接管，断言旧 Worker 的供应商调用数为 0，取消终态或新租约状态保持不变；另测在途返回不覆盖取消。

### P1-8 工作流保存不丢修改，发布等待最新草稿（阻塞）

定位：`apps/web/components/workflow-studio.tsx:279`、`:447`。

已复现：保存 A 期间编辑 B，B 的防抖回调因 `saving.current` 被跳过；A 返回后无条件清除 dirty，界面显示“已保存”，服务端仍为 A。另一个场景中，发布调用的 `await saveNow()` 没有等待正在执行的保存，发布了旧稿。

- [ ] 按工作流与编辑版本追踪保存快照，串行保存并补交请求进行期间的新修改；只有当前修改确实已保存时才能清除 dirty。
- [ ] 发布必须等待全部待保存修改成功落库，保存失败时不得继续发布或显示成功。
- [ ] 使用受控 Promise 和定时器覆盖慢保存期间二次编辑、防抖先触发/后触发、保存中点击发布与保存失败；断言最新图已持久化且发布版本一致。
- [ ] 补组件行为或浏览器回归，确认“已保存”提示与实际持久化内容一致，不仅依赖提取回调的逻辑复现。

### P1-9 Redis 入队状态回写不得覆盖 Worker（阻塞）

定位：`apps/api/app/services/job_service.py:143`。

已复现：先向队列发送消息，Worker 已在另一 Session 提交 `COMPLETED`，入队函数随后无条件提交 `QUEUED`，覆盖完成状态并保留完成时间。短任务与工作流节点可以进入该窗口。

- [ ] 调整持久化与消息发布顺序，在消费者可执行前记录待入队状态；发布后的成功/失败处理不得无条件覆盖已推进的任务状态。
- [ ] 对必要的后续写入使用状态/租约条件更新，并处理发布失败或结果不确定的恢复路径，避免重复派发。
- [ ] 跨 Session 回归覆盖 Worker 在入队函数返回前进入活动态、完成或取消；断言生产者不回退状态、不清除新租约，状态与时间字段一致。

### P1-10 LOCAL 与 AUTO 回退真正执行可重试调度（阻塞）

定位：`apps/api/app/services/job_service.py:197`、`apps/api/app/worker_tasks.py:1827`。

已复现：可重试 `RATE_LIMIT` 将任务重置为 `WAITING` 后重新抛出，本地执行循环随之退出并移除已提交标记；只执行一次且未到次数上限，任务无人继续调度。当前恢复扫描只在 API 启动时执行。

- [ ] 本地执行器捕获可重试失败，采用受控退避重新执行或重新调度，不能只改状态后退出。
- [ ] 每次重试遵守取消、租约、项目并发和最大尝试次数；不可重试错误或次数耗尽统一收敛为失败，不重置已消耗次数。
- [ ] 分别验证 LOCAL 与开发环境 AUTO 回退：假供应商首次可重试失败、随后成功，无需 API 重启或人工干预即可完成；补取消、不可重试和次数耗尽用例，验证任务及关联节点状态。

### P1-11 RQ 并发名额不足时保留后续调度（阻塞）

定位：`apps/api/app/worker_tasks.py:239`、`:1728`。

已复现：项目名额被占用时，第二个任务保持 `WAITING / CONCURRENCY_LIMIT`、尝试次数为 0，而 `execute_job()` 正常返回；RQ 将正常返回视为成功，不因等待名额自动重试。释放名额本身也不会唤醒该任务。

- [ ] 明确区分“任务已终止/无需执行”和“暂时无并发名额”，后者必须进入可追踪的延迟重调度，不得静默成功出队。
- [ ] 保证名额等待不消耗实际模型调用尝试次数，并正确响应取消、避免忙循环和重复入队；若采用 RQ 延迟机制，同步核对调度器启动配置。
- [ ] 回归覆盖同项目两任务、名额为 1、多个 Worker：第二个任务先等待，首个完成释放名额后自动执行，整个过程无需重启或手动重试。
- [ ] 使用独立 Redis/RQ Worker 与假供应商补集成验收，并以真实 PostgreSQL 验证项目行锁下的槽位行为；记录未具备环境的部分，不把 SQLite 或模拟队列结果冒充集成通过。

### P1-12 质检结果完整性与生产门禁一致（阻塞）

定位：`apps/api/app/worker_tasks.py:1614`、`apps/api/app/services/page_completion.py:68`。

已复现：只有 `CONTINUITY` 通过记录即可得到 `production_ready = true`；请求默认五类检查但模型返回 `items: []` 时，没有任何检查记录仍被判为生产通过。当前门禁仅依赖 `INSPECTED / PASSED` 汇总状态，没有核对最新类别记录。

- [ ] 校验供应商输出覆盖全部请求类别、结果值合法，空列表或缺项不得写入“整页检查通过”状态。
- [ ] 生产门禁读取与当前候选、分镜确认状态匹配的最新检查，要求 `SPEAKER`、`CHARACTER`、`OUTFIT`、`PROP`、`CONTINUITY` 五类齐全且全部通过；局部检查不能覆盖其他类别的失败。
- [ ] 缺项返回 `QUALITY_INSPECTION_REQUIRED`，最新结果失败返回 `QUALITY_REVIEW_REQUIRED`；下一页生成、页面/章节导出与工作流输出共用判定。
- [ ] 回归覆盖空输出、缺类别、单类别复检、已有失败后局部通过、历史通过后最新失败、分镜变化后复检，以及五类最新结果通过；对未满足门禁的生成/导出接口断言拒绝。

P1-7～P1-12 共同完成条件：每项先保留失败复现为自动回归，再实现修复；全部完成后执行 `npm run check` 和 `npm run check:full`，并记录独立队列/数据库集成验收边界。默认只使用隔离数据与假供应商，不新增真实付费调用。

## P2：可维护性与运营完善

### P2-1 拆分超大模块

当前主要热点：`project-workspace.tsx` 约 1491 行、`workflow-editor.tsx` 约 1018 行、`worker_tasks.py` 约 1606 行、`workflow.py` 约 1576 行、`workflow_engine.py` 约 1342 行。

- 前端按章节选择、资产绑定、生成工作台、质检修复、任务中心拆分组件与 hooks。
- 后端按任务类型拆分 handler，把 claim/lease/failure convergence 留在统一执行外壳。
- 每次只拆一个边界并补回归测试，不与 P0 状态语义修复混在同一提交。

### P2-2 补足前端行为测试

PR #5 审查时有 17 个 Vitest 测试、165 个 Python 测试；仍需按关键用户行为补足前端覆盖，不只比较测试数量。

- 优先覆盖生产门禁提示、旧候选复检、检查/修复轮询、导出阻塞、任务取消/重试和供应商错误展示。
- 对错误、空数据、慢任务和并发 mutation 建立稳定的用户可见回归测试。
- [ ] 同步改善后端离线夹具：由测试显式提供无真实密钥的“供应商已配置”前提，不再隐含依赖开发机 `.env` 中的凭据文件存在；无凭据环境下本轮曾有 10 项测试因此失败。

### P2-3 模型调用审计与成本可见性

`GenerationRecord` 能保存成功调用的 usage，但当前没有独立的 `ModelCallAttempt`；任务列表的 `estimated_cost` 仍返回 `None`。失败、重试、路由切换和每次尝试的成本不可完整追溯。

- 记录每次调用尝试的供应商、模型、request id、开始/结束时间、耗时、usage、错误码、是否重试和关联 job。
- 根据供应商定价配置提供“估算”而非伪精确成本，并在 UI 中区分估算值与账单值。
- 日志和导出继续禁止保存密钥、认证头和完整凭据路径。

### P2-4 上传与图片资源硬化

- 以 Pillow 实际识别格式为准规范化扩展名和 MIME，不信任客户端文件名与 `content_type` 的组合。
- 增加最大像素数、宽高边界和解压炸弹测试；20 MB 字节限制不能替代像素限制。
- 对缩略图失败、文件落盘成功但数据库提交失败等路径做孤儿文件清理测试。

### P2-5 备份与恢复演练

- 为 SQLite + `storage/generated` + `uploads` 提供一致性备份脚本和恢复检查清单。
- 至少演练一次新目录恢复、Alembic 升级、外键检查、资产文件存在性抽检和页面导出。

### P2-6 统一首页“可用模型数”口径（PR #5 非阻塞后续项）

- [ ] 让 `projects.py::_ai_overview` 的模型统计复用 `/models` 的可用性条件：模型、连接和供应商启用，兼容协议连接具备可写凭据及未冷却的启用密钥；保留原生 Vertex 分支语义。
- [ ] 补充无密钥、密钥禁用/冷却、连接禁用、供应商禁用、凭据只读、模型禁用和原生 Vertex 的回归测试，确保首页计数与模型目录一致。
- [ ] 保持首页仅请求 Dashboard 与 Vertex status，不为修复统计重新增加独立的 `/models`、`/providers` 首屏请求。
- 审查已用内存数据库复现：上述部分不可用场景中新聚合返回 1，原 `/models` 可用计数为 0。影响仅为首页展示，不改变实际选模、路由或生成行为。
- `configured_connection_count` 与供应商列表当前口径一致，无需新增健康或冷却条件；“已配置”不等于“当前可用”。

### P2-7 确认非文本色值变更范围（低优先级）

- [ ] 确认 `globals.css` 的 `.switch-setting i::after` 关闭态滑块背景从 `#817b70` 改为 `#66604f` 是否有意保留；若仅修文字对比度则恢复该处背景色。
- SVG 与 `currentColor` 边框会随文字颜色变化；当前未发现功能性误伤，开启态仍有独立绿色及位移规则，不作为合并阻塞项。

### P2-8 同一工作流并发发布版本冲突（建议）

定位：`apps/api/app/services/workflow_engine.py:502`、`apps/api/app/api/routes/workflow_definitions.py:204`。关联 P1-5，按本轮已确认影响单独跟踪。

已复现：两个独立 Session 同时读取 `MAX(revision) + 1`，一个发布 revision 1，另一个触发 `(workflow_id, revision)` 唯一约束异常；API 仅捕获 `ValueError`，该异常进入服务器错误路径。唯一约束避免了重复版本，主要影响是并发请求失败，不是数据损坏。

- [ ] 对同一工作流的版本分配采用受控锁、数据库级分配或唯一键冲突有限重试。
- [ ] 冲突无法在限次内解决时返回明确的冲突响应而非未处理的服务器错误；失败事务正确回滚，不破坏已发布版本指针。
- [ ] 两个独立 Session 并发发布回归：成功版本 revision 唯一，发布指针有效，另一请求成功重试或明确返回冲突；不得返回未处理的 500。覆盖 SQLite，并在具备环境时验证 PostgreSQL。

## 推荐执行顺序

1. 优先完成 P1-7、P1-9、P1-10、P1-11，收口取消、入队与两种执行器的调度语义，补齐 P0-1 的可靠性闭环。
2. 完成 P1-8 草稿保存/发布一致性与 P1-12 五类质检门禁，防止用户修改丢失和未检页面被放行。
3. 完成 P2-8 已复现的并发发布冲突，继续核对 P1-5 其他序号路径，不把未复现的风险直接视为缺陷。
4. 将全部复现固化为回归，重新执行 `npm run check`、`npm run check:full`，补独立 Redis/RQ、PostgreSQL 与前端交互证据；历史 `cb324e3` 全绿不代表新增场景已通过。
5. 补 P1-6 独立 Lighthouse/FPS 门禁；以小提交处理 P2-6 首页计数，P2-7 样式范围确认低优先级推进，保留其他既有 P2 待办。
6. 主分支通过上述验收后，再做一次用户明确授权的单候选真实供应商验收；不自动删除分支、不触发额外付费生成。

## 稳定版本完成定义

只有同时满足以下条件，才把当前私有 MVP 标记为稳定：

- P0-1 补充闭环及 P1-7～P1-12 均完成，不存在已知的取消后继续派发、旧 Worker/入队方覆盖状态、任务无人调度、草稿丢失或质检误放行问题。
- P2-8 并发发布冲突受控，P1-5 其他序号风险完成范围核对与必要修复；上述新增回归持续通过。
- `npm run check` 与全部 Playwright/Axe E2E 通过。
- Alembic head 校验、全新安装和旧库升级均通过。
- 假模型完整 DAG 从导入运行到人工采用、五类检查和输出节点，状态证据一致。
- 在用户明确授权后，真实供应商只生成一个 1K 候选，确认轮询、采用、检查和导出闭环；不自动生成第二页，不自动修复。
